/**
 * Moondust config. Earned via level-ups and rare Radiant harvests; no
 * spending yet (values provisional, tuned later).
 */

/** Moondust granted per level gained. */
export const MOONDUST_PER_LEVEL = 1;

/** Per-harvest chance a harvest procs as Radiant. */
export const RADIANT_CHANCE = 0.02;

/** A Radiant harvest yields this many units of the crop instead of 1. */
export const RADIANT_YIELD_MULT = 5;

/** Chance a Radiant harvest also grants +1 moondust. */
export const RADIANT_MOONDUST_CHANCE = 0.5;
