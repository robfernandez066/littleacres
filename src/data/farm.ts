/**
 * Starting farm layout: a 4x3 grid of 12 plots. Later tasks (expansions)
 * should change these values rather than hardcoding sizes in scenes.
 */
export const FARM_COLS = 4;
export const FARM_ROWS = 3;
