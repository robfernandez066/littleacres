# AGENTS.md - Little Acres (non-Claude agents)

Read and follow **CLAUDE.md** at the repo root. It is the single canonical
developer guide for this project: standing rules, coding conventions, stable
design invariants, and the mandatory end-of-task report template. Everything
there applies to you unchanged.

One addition for reviewers: when the user explicitly requests a READ-ONLY
game review, you may read the files under docs/ that the request names.
You must not modify those files, and every other CLAUDE.md rule (never
commit or push, one task at a time, report template) still applies.
